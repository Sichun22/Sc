services:
  flask-mvp:
    build: .
    container_name: flask-mvp
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      FLASK_ENV: development
      PORT: 5000
      DATABASE_URL: sqlite:////app/todo.db
